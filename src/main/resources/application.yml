spring:
  application:
    name: code-generator-app
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ai-app
    username: root
    password: gxr96805
server:
  port: 8123
  servlet:
    context-path: /api

# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.gxr.codegenerate.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn

